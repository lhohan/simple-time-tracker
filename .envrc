use flake

#
# Serena MCP Integration for Claude Code
#
# Add project-local bin directory to PATH so our claude wrapper
# takes precedence over the global claude command
#
export PATH="$PWD/bin:$PATH"
